{
  "compilerOptions" : {
    "lib": ["ES2022", "DOM"],
    "module": "ES2022",
    "moduleResolution" : "Bundler",
    "target": "ES2022",

    // Pretty close to enabling; fixes required in `./animation/AnimationScheduler`.
    "strict": true,
    "strictNullChecks": true,
  },
  "include": [
    "./**/*"
  ]
}
